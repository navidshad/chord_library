services:
  website:
    image: ghcr.io/navidshad/chord_library:${SERVER_IMAGE_NAME:-website:latest}
    # build:
    #   context: .
    #   dockerfile: server.Dockerfile
    restart: always
    ports:
      - "8080:8080"
    volumes:
      - ./public:/app/public/downloads
    environment:
      - ADMIN_EMAIL=${SERVER_ADMIN_EMAIL}
      - ADMIN_PASSWORD=${SERVER_ADMIN_PASSWORD}
    depends_on:
      - "mongo"

  mongo:
    image: ghcr.io/navidshad/chord_library:mongodb:6.0.13-jammy
    ports:
      - "27017:27017"
    volumes:
      - ./data:/data/db
    restart: always
